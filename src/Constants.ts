/**
 * Canvas GC is a process which will remove all already deleted items from the
 * array. This constants determines how many trash needs to exist until a
 * Canvas GC is started.
 */
export const CANVAS_GC_AT = 5;

/**
 * The space between two characters when rendering text.
 */
export const CHARACTER_SPACE = 1;

/**
 * How many pixel is produced from each pixel when generating perspective
 * texture in each dimension.
 */
export const PERSPECTIVE_TEXTURE_DETAIL = 8;